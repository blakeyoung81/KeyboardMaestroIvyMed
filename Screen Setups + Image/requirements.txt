# Additional requirements for screen OCR to patient image generation
# These should be installed in your existing sd-venv

pytesseract>=0.3.10
Pillow>=10.0.0
openai>=1.0.0
duckduckgo-search>=6.2.6

# System requirements (install via homebrew):
# brew install tesseract
